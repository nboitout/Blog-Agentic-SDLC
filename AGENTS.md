# Repository workflow

- After completing and verifying requested repository changes, commit them and push them to `main` unless the user explicitly requests a different branch or asks not to push.
- Sync `main` with `origin/main` before editing and do not overwrite unrelated local or remote changes.
